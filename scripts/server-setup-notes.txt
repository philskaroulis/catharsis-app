
$ virtualenv-3.3 env --distribute
$ . env/bin/activate
$ pip install gunicorn
$ pip install pymongo
$ pip install bottle
$ pip install requests
$ echo 'web: gunicorn --log-file gunicorn.log api:app' > ./Procfile
$ pip freeze > requirements.txt
$ echo 'python-3.3.0' > runtime.txt
$ (echo 'env' && echo '*.pyc') > ./.gitignore
$ git init
$ git add .
$ git commit -m "init"


$ heroku create
    http://dry-tundra-4840.herokuapp.com/ | git@heroku.com:dry-tundra-4840.git
    Git remote heroku added
$ heroku addons:add mongohq:sandbox --app dry-tundra-4840

createCollection("​playlists")

$ git push heroku master


